/**
 * @module @/components/ui
 * @description Barrel export for base UI components (shadcn/ui + custom)
 * @layer Presentation (Layer 2)
 */

// Future exports will be added as modules are implemented

export {};
