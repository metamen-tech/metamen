/**
 * @module @/types
 * @description Barrel export for shared TypeScript types and branded types
 * @layer Shared (Layer -1)
 */

// Future exports will be added as modules are implemented

export {};
